<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bettafish">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logobunder"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logobunder"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".custompopup_kukuh"></activity>
        <activity android:name=".custompopup_anggar" />
        <activity android:name=".custompopup_amin" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/SplashScreenStyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".About" />
        <activity android:name=".DetailListIkan" />
        <activity android:name=".MainActivity" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>